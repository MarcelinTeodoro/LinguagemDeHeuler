package main.java.org.cmt.compilers.bytecode;
/**
 * Um wrapper para todos os tipos de valores que a linguagem Heuler pode
 * representar em tempo de execução (números, strings, booleanos, nil, etc.).
 * * Usar 'Object' do Java é a forma mais simples de implementar
 * a tipagem dinâmica em Java.
 */
public class Value {
    // Por enquanto, podemos usar um apelido (type alias) simples
    // ou apenas usar 'Object' diretamente.
    // Para clareza, vamos apenas usar 'Object' onde for necessário
    // e esta classe será mais importante para a tabela de constantes.

    // Esta classe será mais desenvolvida quando implementarmos o interpretador.
    // Por agora, o mais importante é a classe 'Chunk'.
}
